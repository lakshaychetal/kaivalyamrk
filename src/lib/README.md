# `lib/` — Shared helpers

Cross-cutting utilities shared across layers (formatting, type guards, small
helpers). Keep vendor-specific logic in `integration/` and pure business logic
in `domain/`.

Populated as needed.
